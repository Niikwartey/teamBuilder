<%= form_for @survey do |f| %>
    <%= f.fields_for :responses do |r| %>
      Question: <%= r.object.question.text %><br>
      Answer: <%= r.text_field :answer %><br>
      Importance: <%= r.text_field :importance %> <br>

    <% end %>
  <%= f.submit %>
<% end %>
